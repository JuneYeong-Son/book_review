# 아직 구현하지 못한 것 / 향후 작업 (Backlog)

> 요청받았지만 아직 미구현이거나, 나중에 하기로 한 항목들. 완료되면 여기서 지우고
> requirements.md / design / logic 문서로 옮긴다.

## 진행 예정 / 요청됨
- [ ] **공지사항 만들기**: 운영자가 공지를 올리고 사용자에게 노출(홈 배너/목록 등). 관리자 작성·수정·삭제.
      설계 초안: `Notice { id, title, body, pinned, createdAt }` + 관리자 CRUD API + 홈/전용 페이지 노출.
- [~] **안드로이드 앱(스토어 목표)**: Capacitor **번들 + 토큰 인증**으로 전환 완료(`frontend/android/`). 아이디 로그인·회원가입 앱에서 작동(토큰). **남은 것:** ①Android Studio로 서명 AAB 빌드 ②개인정보 처리방침 + Data Safety ③스토어 등록. 가이드 [../android/build.md](../android/build.md).
- [ ] **앱 소셜 로그인(딥링크)**: 카카오/구글을 앱에서 쓰려면 커스텀 스킴 딥링크 + 콜백 토큰 반환 필요(현재 앱은 버튼 숨김).
- [ ] **개인정보 처리방침 페이지**: 스토어 제출 필수(이메일·휴대폰 수집 명시).
- [ ] **iOS 앱**: macOS+Xcode 필요. `npx cap add ios`로 동일 확장.
- [ ] (선택) **첫 소셜 로그인 시 약관·닉네임 확인 화면**: 현재는 자동 가입+동의 자동 기록. 명시적 확인 단계 추가.

## 보안 후속 (감사 하드닝, 낮은 우선순위)
> 상세는 [../security/audit-2026-07-17-run-1.md](../security/audit-2026-07-17-run-1.md) 참고.
- [ ] 이메일 인증 코드 **이메일별 실패 횟수 잠금**(현재 IP 레이트리밋만).
- [ ] `POST /books/import`가 클라이언트 후보를 그대로 신뢰 → **ISBN 서버 재조회**로 카탈로그 오염 방지.
- [ ] 프론트 정적 사이트 **CSP 헤더**(현재 API에 기본 보안 헤더만 적용).

## 향후 고도화 (나중에)
- [ ] **추천 – 임베딩 기반**: 책/사용자 임베딩으로 유사도 추천. (현재는 CF + 콘텐츠 기반)
- [ ] **연령대 추천 UI 복구**: 백엔드(age_group)는 있으나 UI에서 잠시 제거함. 데이터 쌓이면 복구.
- [ ] **책 컬렉션/큐레이션 타이틀**: "서울대 추천도서 100선", "요즘 인기있는 도서" 등.
      설계: `Collection { id, title }` + `Book` N:M.
- [ ] **ML 사용자 태그**: 클러스터링/딥러닝으로 "해외문학 선호", "철학가", "비문학 애호가" 등 취향 태그 부여.
      피처: 읽은 책의 genre/category/author 분포.

## 완료됨 (참고)
- [x] **신고 버튼**: 서평/토론 신고(`POST /reports`, Report 모델). 관리자 대시보드에서 집계·삭제.
- [x] **마이페이지 내 책 편집**: 책별로 관심 지정 토글 + 서평 날짜별 수정/삭제(`PATCH/DELETE /progress/:id`).
- [x] **관리자 페이지(/admin)**: 오늘의 접속자 수·전체 회원 수·신고된 게시물 수, 신고 많은 게시물 삭제.
      관리자 지정은 env `ADMIN_USERNAMES`(**기본값 없음** — 보안 감사 후 기본 'reader' 제거). 접속자 집계는 `User.lastSeenAt`.
- [x] **연령대별 추천**: 알라딘이 연령대 미지원 → **우리 플랫폼 자체 데이터**로 연령대별 인기 집계 구현.
      (회원가입/설정에서 출생연도 입력, `method=popular&ageGroup=20` 등)
- [x] **무한 캐러셀**: › 버튼이 끝에 닿으면 서평/토론/베스트셀러를 계속 더 불러옴(페이지네이션).
- [x] **배포**: Render(백엔드+프론트+Postgres). 로컬 SQLite / 배포 Postgres 자동 전환.
- [x] **협업 필터링(CF)**: '읽은 책과 비슷한 책'을 아이템 기반 CF로 대체(+콘텐츠 보충).
- [x] **책 단위 별점**: 서평별 → 책 단위(Rating). 책 상세에서 평균/내 별점.
- [x] **추천 제외(X)**: '추천 안 받을 책' 리스트(RecoExclusion). 특정 책 제외(exclusions).
- [x] **유저 프로필**: `/users/:id`에서 그 유저의 서평·서재·토론.
- [x] **서평 댓글 / 편집·삭제 / URL 개선**(`/books/:bookId/reviews/:번호`).
- [x] **사용자 신고 + 관리자 회원 삭제**.
- [x] **옮긴이만 다른 책 동일 취급**(제목+작가 기준 중복 제거).
- [x] **UI**: 책 이미지 잘림 수정, 책/서평 상세 큰 이미지 레이아웃, 댓글 구분, 내 글귀 탭.

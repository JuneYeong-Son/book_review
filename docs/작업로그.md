# 작업 로그 (Work Log)

> 사용자가 지시한 작업을 기록하는 곳. 세션이 바뀌어도 여기만 보면 무엇을 왜 했는지 알 수 있다.
> **최신 항목을 맨 위에** 쌓는다(역순). 각 항목은 날짜 · 지시 내용 · 변경 요약 · 커밋을 담는다.
> (미구현/백로그는 [todo.md](todo.md), 확정 스펙은 requirements.md / design / logic 로.)

---

## 2026-07-16 — 버셀 컴포넌트 합성 패턴(vercel-composition-patterns) 적용

**지시:** 버셀 프론트엔드 스킬들 적용해줘. (vercel-react-best-practices는 앞선 리팩터에서 대부분 반영 → 이번엔 vercel-composition-patterns)

**한 일:**
- **명시적 변형(patterns-explicit-variants)**: `StarRating`(onChange 유무로 입력/표시 분기)을 `StarRatingInput`·`StarRatingDisplay` 두 컴포넌트로 분리. 표시용은 disabled 버튼 5개 대신 `role="img"`+aria-label 하나로 → a11y 개선.
- [book_reviews_page](../frontend/src/pages/book_reviews_page.tsx)를 두 변형으로 교체.
- **죽은 import 7개 제거**: StarRating이 9개 파일에서 import됐지만 실제 사용은 book_reviews_page 뿐이었음(별점이 서평별→책 단위로 이동하며 남은 잔재). quick_actions·home_page·book_card·my_page·my_book_page·records_page·user_profile_page에서 제거.
- 빌드 통과.

**제외(사유):** BookCard의 `loggedIn`/`interested`는 렌더 "모드"가 아니라 런타임 상태 → 변형 분리 시 이 규모에선 과설계. Modal `title`은 boolean 아님. 공유 fetch 상태(state-lift-state)는 SWR 도입과 함께 별도 작업 권장.

**커밋:** `[refactor] 별점 컴포넌트를 명시적 변형(Input/Display)으로 분리 + 죽은 import 정리`

---

## 2026-07-16 — 디자인 방향 "고요한 서재" 적용

**지시:** 프론트엔드가 어떻게 보일지 시안을 보고 싶다 → 3가지 방향 중 **A. 고요한 서재**(문학잡지풍: 따뜻한 종이 + 숲색·황동, 명조 제목)를 선택.

**한 일:**
- [index.css](../frontend/src/app/styles/index.css) 색 토큰 교체: `--accent` 브라운(#7b5e3b)→숲색(#345c4d), `--bg`/`--surface` 따뜻한 종이 톤, `--gold` 황동(#b98a3c), `--border` 등. `--accent-ink` 추가.
- 타입: `--font-display`(명조: Nanum Myeongjo/Batang) 추가 → `h1,h2,h3,.brand`에 적용. 본문은 `--font-body`(Pretendard).
- 레이아웃: 히어로 하단 2px 실선, 카드/모달 모서리 축소(14→8, 16→10, 버튼 8→6)로 "각진" 느낌.
- `index.html` theme-color를 숲색(#345c4d)으로.
- 참고 시안 아티팩트(3방향 비교) 제작.
- 빌드 통과.

**커밋:** `[style] 디자인 방향 '고요한 서재' 적용 (숲색·종이 톤 + 명조 제목)`

---

## 2026-07-16 — 프론트엔드 Feature-Sliced Design(FSD) 폴더 구조 재편

**지시:** 프론트엔드 폴더 구조를 Feature-Sliced Design에 따르게 해줘. (실용형 수준 선택)

**한 일:**
- `@/` 경로 별칭 설정: `tsconfig.json`(`paths`) + `vite.config.ts`(`resolve.alias`)
- `src/`를 6개 FSD 레이어로 재편(모든 파일 `git mv`, import를 `@/` 절대경로로 통일):
  - `app/` — `App.tsx`(라우팅), `styles/index.css`
  - `pages/` — 13개 `*_page.tsx`
  - `widgets/` — nav_bar, app_footer, reading_calendar, carousel, quick_actions
  - `features/` — report_button, book_import_panel
  - `entities/` — book_card, star_rating
  - `shared/` — api(client·types), ui(modal), lib(auth_context)
- 구조 설명 문서: [design/frontend_structure.md](design/frontend_structure.md)
- 빌드(`tsc -b && vite build`) 통과 확인 (산출물·코드분할 동일)

**커밋:** `[refactor] 프론트엔드 FSD 폴더 구조 재편 (@/ 별칭 + app/pages/widgets/features/entities/shared)`

---

## 2026-07-16 — 프론트엔드 접근성·성능 개선

**지시:** 이전에 설치한 스킬(web-design-guidelines, vercel-react-best-practices)로 프론트엔드를 검토하고 지적된 것들을 전부 고쳐줘.

**한 일:**
- **접근성**: 중첩 인터랙티브 버튼 해소, 아이콘 버튼 `aria-label`, Modal 접근성(role/Escape/포커스/스크롤 잠금), 폼 입력 `aria-label`/`name`/`spellCheck`, 에러 `role="alert"`, 이미지 `width`/`height`+`loading="lazy"`, 전역 CSS(`:focus-visible`·`prefers-reduced-motion`·`color-scheme`·`theme-color`·`tabular-nums`), `...`→`…`, 제목 위계 교정, 로그인 기본 계정정보 제거
- **성능**: 라우트 코드 분할(`React.lazy`+`Suspense`), `useMemo`(home_page·reading_calendar)
- 빌드(`tsc -b && vite build`) 통과 확인

**제외(사유):** SWR 도입(구조 변경·회귀 위험), report_button `window.prompt`→모달(UX 결정 필요), 홈 필터 URL 반영(추천 페이지네이션과 결합).

**커밋:** `[refactor] 프론트엔드 접근성·성능 개선 …`, `[chore] .gitignore에 *.tsbuildinfo 추가`
